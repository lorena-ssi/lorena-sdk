// const chai = require('chai')
// Configure chai
// const expect = chai.expect

describe('Lorena Terminal', function () {
  xit('should create a new wallet and exit', async () => {
  })

  xit('should log in with an existing wallet and exit', async () => {
  })

  xit('should list recipes', async () => {
  })

  xit('should provide help', async () => {
  })

  // etc
})
